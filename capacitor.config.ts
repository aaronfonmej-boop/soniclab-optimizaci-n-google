import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aaron.soniclab3d',
  appName: 'SonicLab 3D',
  webDir: 'dist',
  android: {
    backgroundColor: '#020617',
  },
};

export default config;
